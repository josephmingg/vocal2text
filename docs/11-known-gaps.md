# Known Gaps (v1) — deliberate, tracked

Deferred with intent after the 2026-08-16 cold review; each names its spec anchor and the
planned resolution. Nothing here is silently missing — the code comments cross-reference
this file.

| # | Gap | Spec | Plan |
|---|---|---|---|
| G1 | Secure-field block persists no anonymous "blocked" event (nothing is stored at all — the stronger privacy reading) | FR-3.2 | Add a `recordBlockedSecureFieldEvent()` seam on TranscriptStoring in M5 polish |
| G2 | **Half shipped.** The waveform is real — Mac stabilization replaced the synthesized ripple with live microphone levels. Still open: the streaming partial-text preview is a blank placeholder, so no words appear while you speak (committed text is unaffected; transcribe-on-release is the correctness path). The most *visible* gap left on the Mac | FR-4.1 | Wire `engine.transcribeStream` partials into `hudState.partialText` |
| G3 | ~~Per-profile `providerOverride` has no effect~~ — shipped: `Dependencies.selectCleanup` resolves the stage-3 pipeline per take from the pinned profile, and the profile editor exposes an Ollama model override. Only `.ollama` overrides are honored — Settings carries a URL for no other provider, so anything else falls back to the global model rather than failing the take | docs/05 §3.2 | Done (Mac stabilization) |
| G4 | ~~Low-disk recording guard not implemented~~ — shipped: `DiskSpaceGuard` refuses to start a take below 1 GB free, and a mid-take check (~every 4 s) finishes a running take early through the normal stop path — captured audio is transcribed and delivered, and the HUD says why | FR-1.3 | Done (Mac stabilization batch 1) |
| G5 | ~~Escape cannot discard a hands-free (locked) take once the physical press ended~~ — shipped: the decision core tracks lock state (set on `.lockToggled`, cleared by the finishing press or `noteLockEnded()` for the 15-min cap/sleep), and an Escape keyDown with no active press discards the locked take; Linux-tested | FR-1.6 | Done (Mac stabilization batch 1) |
| G6 | Repeated-token-loop collapse rebuilds whitespace as single spaces when a loop is found (long-import paragraph breaks lost in that rare case) | FR-6.2 | Range-splice rebuild preserving separators |
| G7 | ~~FTS5 external-content tables key on a volatile rowid~~ — shipped: migration `v2-transcript-surrogate-rowid` rebuilds `transcript` with `seq INTEGER PRIMARY KEY AUTOINCREMENT` (an INTEGER PRIMARY KEY *is* the rowid, so the numbering is data and survives a rebuild; AUTOINCREMENT also stops a new row from inheriting a deleted row's stale index entry) and re-derives both FTS indexes. VACUUM is now safe and is run after Delete-All, so deleted transcript text leaves the file instead of lingering in free pages | docs/03 §5 | Done (Mac stabilization) |
| G8 | ~~`targetAppName` stays nil~~ — shipped: the Mac store adapter resolves the name via `NSRunningApplication` at save time; a lookup miss keeps the bundle-ID fallback | FR-5.1 | Done (Mac stabilization batch 1) |
| G9 | ~~Audio files are not retained~~ — shipped: a delivered take is encoded to AAC (~3 KB/s) at `Application Support/Vocal/audio/<transcript-id>.m4a`, honoring Settings → "Keep audio"; History plays it back, deleting a transcript deletes its recording, Delete-All clears them, and a launch sweep enforces the window on files already written. Cancelled-take recovery ships too: `RecoveryStore` surfaces the newest sidecar still inside its 24 h window (takes under 0.5 s are skipped as mis-taps) and the menu bar offers it back as "Recover Cancelled Take (⟨length⟩)". `DictationSession.recover` runs the identical post-capture pipeline — the only difference is `source: .recovered`, which History badges — and resolves the profile *now*, so the text lands where the user is typing today rather than where they were when they hit Escape. The recording is deleted only once the session reports it consumed, so a failed transcription leaves the take recoverable for another try | FR-5.1/1.6 | Done (Mac stabilization) |
| G10 | ~~iOS app not yet built~~ — built, and the keyboard extension (docs/02 mode D2b) with it, alongside share-sheet import and the Live Activity. All of it compiles in CI; **none of it has run on a phone yet** | docs/02 | Device pass per docs/14; AC-i1/i3/i4/i5/i6 stay open until then |
| G11 | ~~Onboarding hotkey-choice + test-playground pages trimmed~~ — shipped in #12/#14 and this row simply never caught up. `OnboardingView` builds a `.hotkey` step (and a conditional Fn-coexistence page when the chosen key is Fn and Globe is bound), and `HotkeyPickerView` carries the shared preset picker, the custom recorder, advisories, and the live "Test Your Key" tester used in both onboarding and Settings. Unverified on hardware like the rest of the hotkey work — see docs/12 A7 | FR-11.1 | Done; hardware pass tracked by A7 |
| G12 | `AppleSpeechEngine` does not pass `dictionaryTerms` to `DictationTranscriber.contextualStrings` yet; stage 2 is the backstop either way | docs/04 §3 | Wire contextualStrings in the adapter |
| G13 | **Burmese recognition — Mac shipped, iPhone pending.** Pinned မြန်မာ on the Mac routes to `SherpaOnnxEngine` (Omnilingual CTC 1B int8 — 10.78% CER on FLEURS `my_mm`, docs/benchmarks/burmese-asr-2026-08-17.md) via `LanguageRoutingEngine`; first use downloads ~790 MB. Still open: auto mode stays on Whisper by design (routing needs a pin — menu-bar toggle or a per-profile language pin from Settings → Profiles, G17), and the iPhone 300M call waits on on-device RTF. **Real-audio path verified on the owner's Mac (2026-08-17)**: download → extract → pinned decode all worked; built-in mic yields usable Burmese (errors concentrate in names — the dictionary is the fix), while a Bluetooth/AirPods mic (HFP call profile: narrowband + lossy codec + call DSP) degrades output to wrong-script garbage — use the built-in mic for Burmese | docs/04 App. A | Measure 300M RTF on an iPhone, then wire `.omnilingual300M` + an iOS-safe extractor |
| G14 | Zawgyi handling is not shipped at all: the rule-based detector prototype misclassified legitimate Unicode Burmese (မြေပုံ, ငွေကြေး) and real Shan digits, and was removed in the v1.1 review rather than left as dead code | docs/04 App. A | Port google/myanmar-tools (Markov detector + conversion table) when Zawgyi input proves to be a real nuisance |
| G15 | ~~Cleanup provider built once at launch~~ — shipped with G3: the model and server URL are read per take, so a Settings change applies to the next dictation. The HUD privacy badge is computed from the current URL for the same reason | docs/05 §3.2 | Done (Mac stabilization) |
| G16 | ~~Mac first-run hint race~~ — shipped on the Mac: the hint is tagged with a take generation and dropped when stale. **Remaining (iOS, on hold with G10):** iOS can race `.processing` past the delivered `.result` | FR-2.4/FR-i2.1 | iOS half lands with the device pass |
| G17 | ~~No profile editor exists in either app~~ — the Mac Settings → Profiles pane ships it: name/icon, per-profile cleanup opt-in + prompt, language pin (incl. မြန်မာ → Burmese engine), Myanmar digits, spoken punctuation, ZH formatting, app/website routes, priority, and default-profile handling. Profiles now persist (`ProfileBootstrap.loadOrSeed` seeds the built-ins once, both apps; edits apply at the next press, no relaunch). **Remaining:** iOS has no editor (its seeded set is read-only), and `providerOverride` has no UI — pointless until G3/G15's provider-selecting factory exists | docs/05 §4 | iOS editor after the G10 device pass; providerOverride UI lands with G3 |
| G18 | Burmese spoken-punctuation command vocabulary needs native-speaker validation: the Myanmar-script commands were removed after review found them byte-identical to ordinary words (ပုဒ်မ = "section"), and the remaining English commands ship disabled | docs/04 App. A | Validate a command vocabulary with a native speaker, then re-enable per profile |
| G19 | Keyboard profile routing is the manual picker plus the armed session's default — iOS exposes no public host-app bundle ID | FR-i2.2 | `BridgeRequest.hostAppBundleID` already crosses the wire unused, so a future hint needs no protocol change |
| G20 | Share-sheet import accepts `public.audio` only; video containers are filtered out of the activation rule rather than decoded | FR-i2.3 | AVAssetReader path in `AudioFileDecoder` |
| G21 | Stage 3 (AI cleanup) does not run on share-sheet imports — the iOS build injects no cleanup pipeline; the row records `providerUnavailable` | FR-i3.4 | Lands with the Apple Foundation Models provider on iOS |
| G22 | Darwin-notification delivery latency to a backgrounded, audio-active app is unmeasured — the docs/02 §3.1 `[verify]` spike | docs/02 §3.1 | Measure on device per docs/14 §3; commit to `docs/benchmarks/` |
| G23 | An armed capture session holds a live input tap for its whole window, so the orange indicator stays lit and battery is spent. Deliberate (it is the only residency mechanism iOS offers) and disclosed in the arming UI, but the battery cost is unquantified | FR-i1.3 / NFR-i3 | docs/14 §7 |
| G24 | **AC-i3 and AC-i5 are unreachable on a free Apple ID.** A personal team cannot provision App Groups, which the keyboard and share extensions exist to cross, so those two acceptance criteria stay open unless the owner enrols. `make generate-free` builds the signable subset — app, Action Button, history, dictionary, Dynamic Island | docs/02 AC-i3/i5 | Paid Developer Program enrolment, or accept mode D1 (dictate → auto-copy → paste) as the shipping answer |
